## 2024-05-19 - Missing ARIA Labels on Icon-Only Buttons
**Learning:** Found an accessibility pattern specific to the marketing showcase components where decorative icon-only buttons (`ArrowUp`, `Plus`) lacked `aria-label` attributes.
**Action:** Always ensure that icon-only interactive elements in marketing pages, such as the product showcase, have explicit `aria-label`s for screen reader accessibility.
